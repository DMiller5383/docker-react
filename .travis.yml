language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t dmiller/docker-react -f dockerfile.dev . 
script:
  - docker run -e CI=true dmiller/docker-react npm run test    